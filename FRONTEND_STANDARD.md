# MomentMaker 前端开发与联调标准

更新日期：2026-09-04。

本文件根据现有 `需求原文.md`、团队提供的后端技术文档和本地静态页面整理。记录项目开发约定，不代表接口已部署、字段已全部确认或真实联调已通过。此次仅更新文档，不迁移或修改页面代码。

## 1. 技术栈与阶段

| 阶段 | 技术栈 | 定位 |
|---|---|---|
| 当前静态版 | HTML + CSS | 五页视觉与响应式基线；保持现状 |
| 后续联调版 | React + Vite + TypeScript | 页面、跨页状态和业务交互 |
| 联调版样式 | Tailwind CSS，必要时保留普通 CSS | 响应式布局；现有 CSS 插画和视觉不必为迁移而重做 |
| 联调版请求 | Axios + REST API | 集中封装接口、错误和 Mock 降级 |
| 进度通信 | 每 2 秒轮询状态接口 | 本轮范围不引入 SSE 或 WebSocket |

技术文档开头写“SSE/轮询”，协议章节明确“每 2 秒轮询”，本项目按后者实施。当前 HTML/CSS 限制适用于静态阶段；技术栈迁移是下一次开发任务，不能因本标准更新而自动执行。

后端约定为 Python + FastAPI + SQLite + python-multipart，文件存本地磁盘；AI 模块集成于后端，使用 rembg、OpenCV/ffmpeg、Pillow。前端只通过后端接口访问，不运行 AI 运算、不直接读数据库或服务器磁盘。本次未核验这些后端组件是否实际存在。

## 2. 已有工作及真实完成范围

| 页面 | 当前已有 | 联调阶段仍需实现 |
|---|---|---|
| P1 `index.html` | 首页、6 张 CSS 排版样例、跳转入口 | 真实作品列表、分页、详情预览、发布后刷新与分享定位 |
| P2 `upload.html` | 原生文件选择、描述输入、样例入口 | 文件校验、缩略图、删除、上传、文件 ID 与描述传递 |
| P3 `template.html` | 模板单选、风格单选、物料多选 | 选项与预览绑定、跨页保存、创建任务、防重复提交 |
| P4 `editor.html` | 名称输入、装饰元素勾选、固定画布样例 | 真实抠图元素、轮询、删除/替换/排序、合成提交 |
| P5 `preview.html` | 固定成品、吧唧及小票 CSS 样机 | 真实结果、多页预览、导出、发布、分享 |

静态版不存在真实上传、AI、导出、发布、数据持久化或跨页状态。当前元素为装饰样例，不是后端抠图结果。历史页面检查记录见 README 和 checks；不能据此宣称功能联调完成或真机兼容通过。

继续沿用现有视觉、五页信息结构和桌面/手机适配。原型中的“我的”和草稿不自动加入范围。账号体系、实体下单和趣味玩法不属于当前 MVP 必做项。

## 3. 接口映射：已知与待确认

接口前缀统一 `/api`。业务响应为 JSON；文件上传的请求编码和字段仍需确认，不能把“响应 JSON”误解为文件也使用 JSON 上传。

| 页面/动作 | 接口 | 技术文档明确的信息 | 接入前必须确认 |
|---|---|---|---|
| P1 广场 | `GET /api/works` | 支持分页、返回作品 | 分页参数、列表包装、总数或下一页判断、排序 |
| P2 上传 | `POST /api/upload` | 返回文件 ID 列表 | multipart 字段名、单/批量、响应字段名、部分失败、视频结果 |
| P3 开始生成 | `POST /api/task/start` | 提交模板、风格、文件 ID；返回 `task_id` | 请求键名、类型/风格枚举、描述与物料字段、重复请求处理 |
| P4 进度 | `GET /api/task/{task_id}/status` | 每 2 秒轮询；示例结构见下 | 状态枚举、终态、错误字段、元素何时可编辑、任务失效规则 |
| P4 合成 | `POST /api/task/{task_id}/compose` | 确认元素后合成，返回成品 URL | 元素 ID/顺序/替换的提交格式、同步或异步、结果字段、多页结果 |
| P5 发布 | `POST /api/works` | 将作品发布到广场 | 请求与响应结构、作品名称、昵称、结果关联、作品 ID 和分享 URL |

技术文档给出的状态示例：

```json
{
  "task_id": "abc123",
  "status": "processing",
  "progress": 60,
  "elements": [
    { "id": "elem_1", "url": "/files/elem_1.png", "label": "人物1" }
  ],
  "result_url": null
}
```

作品示例：

```json
{
  "work_id": "work_001",
  "cover_url": "/files/cover.jpg",
  "template": "comic",
  "style": "anime",
  "nickname": "游客用户",
  "likes": 42
}
```

只有示例中的 `processing`、`comic`、`anime` 被文档展示；不能据此擅自认定完整枚举。现有页面的其他选项须建立经确认的 API 映射。当前作品示例没有作品名称、详情、多页图片、物料样机、分享链接；这些能力需要补字段或补接口，不能冒用封面当作全部结果。

联调前还需后端提供：可访问的测试地址、实际 OpenAPI/接口说明、各接口成功与失败示例、图片 URL 规则、CORS 配置。上传请求若采用 FormData，应由浏览器生成 multipart boundary。

## 4. 后续前端编码要求

### 结构和职责

联调版采用技术文档建议的 `frontend/` 目录；当前文件位置本次不变。

```text
frontend/
  src/
    pages/       # 五个页面
    api/         # Axios、请求/响应类型、协议适配
    mock/        # 与接口结构一致的本地数据
  public/
    mock/        # 本地可用的样例图片
  vite.config.ts
```

- 页面不得各自硬编码 API 地址、散落请求逻辑或构造假成功结果。
- API 数据类型以确认的后端契约为准，展示用状态与服务端原始状态分开。
- 跨页保存文件 ID、描述、模板/风格、物料、任务 ID、元素选择/顺序和结果；真实任务与样例任务隔离。
- 返回上一步保留本次选择。刷新丢失任务时展示可理解的恢复/重新开始入口，不崩溃、不默认声称生成成功。
- 只抽取实际复用的组件，不提前加入复杂状态库、账号系统或通用平台层。

### URL 与静态资源

- `/files/...` 是后端资源路径；前后端不同源时必须解析到后端资源地址，或通过明确的代理转发。
- 开发环境同时处理 `/api` 与 `/files`；生产环境明确同源反向代理或跨域访问方案。
- 不把本机磁盘路径直接放进浏览器图片地址。API 地址与资源地址支持环境配置。
- Canvas 导出涉及跨域图片时，需要后端/CDN 提供相应 CORS；跨域下载和微信内保存方式需实测。

### 请求与状态

- UI 至少区分未开始、上传中、处理中、可编辑、合成中、成功、失败、超时及演示模式；这些是前端体验状态，不是擅自规定后端枚举。
- 轮询目标间隔为 2 秒，不重叠请求；完成、失败、超时、离开页面或切换任务时停止并清理旧请求。旧任务响应不得覆盖新任务。
- 提交期间禁用重复操作。错误说明真实原因并提供恢复入口；复制失败、保存失败、发布失败不得提示成功。
- 后端尚未返回进度时可显示等待动画，但不得将计时动画当作 AI 已完成的证据。
- 未确认幂等规则前，不自动重试上传、创建、合成、发布等写请求；超时不等于服务器没有执行。
- 上传校验覆盖图片最多 20 张、单张 10MB；MP4 最长 60 秒、100MB。HEIC 预览/转换支持及混合上传数量规则仍需确认。

## 5. Mock、超时与不一致处理

| 文档中的不一致/缺口 | 本项目标准 |
|---|---|
| 超过 3 秒再降级，但要求 0.5 秒响应 | 显式演示入口在样例就绪后 0.5 秒内展示；自动降级另计检测耗时，不能宣称总耗时 0.5 秒 |
| Mock 图片只指向 CDN，但要求断网可用 | 关键 Mock JSON 和图片随前端打包；CDN 可用于在线分发，不能是断网唯一依赖 |
| 每个接口 3 秒超时，但上传和合成可能耗时较长 | 3 秒作为短读请求降级触发参考；上传/合成的请求时限和服务端执行方式需协商，不统一硬套 |
| PRD 的 60 秒 AI 超时与接口超时混用 | AI 任务等待总时限按 60 秒验收；单次 HTTP 时限独立，起算点和合成任务覆盖范围联调时明确 |
| `?demo=true` 是后端可选方案 | 未验证前不依赖，不把它当作已实现接口 |
| 静态计时动画与真实进度混淆 | 真实模式按任务状态显示；演示模式明确标识，不产生真实发布成功提示 |

切换 Mock 后仍应能体验完整样例链路，但不得把 Mock ID 用于真实后端合成或发布。演示发布只能表达为演示效果。400/422 等输入错误应展示校验信息，不直接用 Mock 掩盖。

“已加载站点断网后使用样例”与“完全断网首次打开站点”是不同要求。本地样例资源不等于离线缓存；后者需要站点预装或明确缓存方案，不在未实现时宣称支持。

## 6. 分阶段验收门槛

| 阶段 | 必须提供的验收证据 |
|---|---|
| 静态视觉 | 五页可访问、导航正确、桌面和手机截图；明确未接入能力 |
| 技术栈迁移 | React/TypeScript 构建通过；保留五页视觉和响应式；不把迁移等同联调完成 |
| P1/P2 真实接入 | 真实分页、空/失败状态；上传返回文件 ID，校验/删除/重试符合约定 |
| P3/P4 真实接入 | 真实 `task_id`、状态响应与元素图片；停止轮询、错误/超时恢复；元素调整传入合成 |
| P5 闭环 | 成品来自真实合成；导出可打开；发布成功后广场能查询对应作品；分享定位可验证 |
| 演示与异常 | 显式样例入口、断网/5xx/超时场景；本地素材可用；演示/真实模式无混淆 |

每次只做一组可独立验收的修改。提交说明列出页面/文件、完成能力、验证方式和未完成项。Mock 响应、固定图片、Loading 动画或仅构建通过都不能替代真实链路证据。

### 屏幕与浏览器

- 布局宽度至少检查 320、390、768、1440 像素；电脑宽屏和手机单栏均需截图。
- 页面无横向滚动、文字或控件截断，底部安全区和导航不阻止用户访问操作按钮。
- 真机验收覆盖 iPhone Safari、Android 微信、iPad/平板；PC 覆盖 Chrome。模拟视口不等于真机通过。
- 上传、触摸操作、下载/长按保存、剪贴板均需按目标浏览器验证。
- 控件有可访问名称，键盘可操作，选择/错误状态不能只靠颜色表达。

### 性能与内容

- PRD 首屏及广场首屏样例图加载目标 ≤ 1.5 秒；报告须注明设备、网络和缓存条件，不凭 CSS 体积推断达标。
- 最终广场准备 12 张有效示范作品，首屏前 6 张、后续懒加载；当前只有 6 张 CSS 排版样例，尚非该验收项完成。
- 生成期间持续提供明确反馈；页面不能白屏或无限等待。
- 演示入口目标 0.5 秒与 AI 任务 60 秒上限按第 5 节口径验证。

## 7. 下一次联调前的对齐清单

1. 后端给出运行地址和真实接口契约，逐项补齐第 3 节字段。
2. 确认模板/风格枚举、描述/物料/作品名称如何传递。
3. 确认合成同步或异步、任务终态、错误结构和重复请求处理。
4. 确认结果图、多页、实体样机、分享链接的来源；点赞暂仅展示计数，文档未定义写接口。
5. 确认 Canvas 导出与服务端图片下载的实际交付方式，保留 PRD 导出目标。
6. 然后再安排技术栈迁移、P1/P2 接入、P3/P4 接入、P5 与异常验收。
