# MomentMaker 项目工作约定

## 当前基线

- 先阅读 `FRONTEND_STANDARD.md` 和 `README.md`。原始产品需求见 `需求原文.md`。
- 当前交付是 HTML/CSS 静态五页。后续联调技术栈确定为 React + Vite + TypeScript + Tailwind CSS + Axios，通信采用 REST + 每 2 秒轮询。
- 技术栈迁移未执行，不能把规范更新视为启动开发的指令。用户要求仅更新文档时，不修改页面、样式、依赖或后端代码。
- 后端文档是待核对的契约来源，不代表接口已上线。未知字段与枚举按标准中的待确认项处理，不编造为既定事实。

## 开发要求

- 保留现有五页结构、视觉方向和桌面/手机响应式。改动须对应用户任务，先说明范围和验收方法。
- 后续请求集中于 API 层，资源 URL 正确指向后端，真实与 Mock 数据隔离。
- 上传、创建、合成、发布不得用固定反馈冒充成功；未确认幂等性不自动重试写请求。
- 轮询结束和离页时清理；生成超时、异常恢复和 Mock 时间口径遵守 `FRONTEND_STANDARD.md`。
- 不自行扩展账号、草稿、个人中心、实体下单或 AI 运算职责。

## 验证与交付

- 页面布局检查 320/390/768/1440 宽度；视觉变化需截图核对。
- 区分静态、Mock、真实接口及真机验收。真实上传到发布的完成状态须有接口与结果证据。
- 每次报告修改文件、验证结果和未完成项；只更新文档时明确声明未进行接口实测。
